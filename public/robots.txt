# Served at https://localsme.supernovasearch-localseo.workers.dev/robots.txt
#
# This Cloudflare Worker is served from its own domain root, so the file
# sits where crawlers expect it. (Under a sub-path deployment it would need to
# move accordingly, or it is ignored.)

User-agent: *
Allow: /
Disallow: /admin/

Sitemap: https://localsme.supernovasearch-localseo.workers.dev/sitemap-index.xml
