# Served at https://creativedigitalgrowth.pages.dev/robots.txt
#
# This Cloudflare Pages project is served from its own domain root, so the file
# sits where crawlers expect it. (Under a sub-path deployment it would need to
# move accordingly, or it is ignored.)

User-agent: *
Allow: /
Disallow: /admin/

Sitemap: https://creativedigitalgrowth.pages.dev/sitemap-index.xml
